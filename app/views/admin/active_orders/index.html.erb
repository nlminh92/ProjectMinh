<div id="page-wrapper">
  <ul class="nav nav-tabs">
    <li class="active"><a data-toggle="tab" href="#home">Ordered</a></li>
    <li><a data-toggle="tab" href="#menu">Actived and Wait brand</a></li>
    <li><a data-toggle="tab" href="#menu2"> Not active</a></li>
  </ul>
<div class="tab-content">
    <div id="home" class="tab-pane fade in active">
      <div class="col-md-12">
        <table class="table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Retailler Name</th>
              <th>Brand Name</th>
              <th>Total</th>
              <th>View Profile</th>
            </tr>
          </thead>
          <tbody>
              <% @carded.each_with_index do |card, index| %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= card.retailler.name %></td>
                    <td><%= card.brand.name %></td>
                    <td><%= card.all_total %>$</td>
                    <td><%= link_to "View", cart_path(card.id) %></td>
                  </tr>
              <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <div id="menu" class="tab-pane fade in">
      <div class="col-md-12">
        <table class="table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Retailler Name</th>
              <th>Brand Name</th>
              <th>Total</th>
              <th>View</th>
            </tr>
          </thead>
          <tbody>
              <% @caring.each_with_index do |card, index| %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= card.retailler.name %></td>
                    <td><%= card.brand.name %></td>
                    <td><%= card.all_total %>$</td>
                    <td><%= link_to "View", cart_path(card.id) %></td>
                  </tr>
              <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <div id="menu2" class="tab-pane fade in">
      <div class="col-md-12">
        <table class="table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Retailler Name</th>
              <th>Brand Name</th>
              <th>Total</th>
              <th>View</th>
            </tr>
          </thead>
          <tbody>
              <% @carings.each_with_index do |card, index| %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= card.retailler.name %></td>
                    <td><%= card.brand.name %></td>
                    <td><%= card.all_total %>$</td>
                    <td><%= link_to "View", cart_path(card.id) %></td>
                  </tr>
              <% end %>
          </tbody>
        </table>
      </div>
    </div>
</div>
